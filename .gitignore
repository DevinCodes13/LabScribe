# Secrets — the real .env must never be committed (spec R4)
.env

# Local app state (session history + generated docs live next to .env in dev mode)
sessions.json
/generated/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# PyInstaller build output
build/pyinstaller/
dist/
*.spec.bak

# OS / editor noise
Thumbs.db
Desktop.ini
.vscode/
.idea/

# Local capture data if ever pointed inside the repo by mistake
# (leading slash = repo root only; src/labscribe/capture is real code)
/capture/
